# ชุดตรวจหน้าพิมพ์

ให้ทดสอบทั้ง **Google Chrome** และ **Microsoft Edge** โดยตั้งซูมเบราว์เซอร์เป็น **100%** และตั้งมาตราส่วนหน้าพิมพ์เป็น **100% / Actual size**

## หน้าซอง DL, C5 และ A4 แนวนอน

ทดสอบแต่ละขนาดด้วยข้อมูลต่อไปนี้:

- ชื่อผู้รับสั้น 1 บรรทัด
- ชื่อผู้รับยาวเกิน 80 ตัวอักษร และตรวจว่าไม่ล้นกรอบ
- ที่อยู่ 3, 4 และ 5 บรรทัด
- เปิดและปิด `ตราครุฑ`
- เปิดและปิด `ข้อมูลผู้ส่ง`
- เปิดและปิด `กรอบค่าฝากส่ง`
- ตรวจว่าตัวอย่างย่อและหน้าพิมพ์ใช้ตำแหน่ง ขนาด และระยะบรรทัดตรงกัน

## ใบนำส่งไปรษณีย์

ทดสอบจำนวนรายการต่อไปนี้:

| จำนวน | ผลที่ต้องได้ |
|---:|---|
| 1 | 1 หน้า มีแถวว่างครบถึงแถวที่ 30 |
| 29 | 1 หน้า มีแถวว่าง 1 แถว |
| 30 | 1 หน้าเต็ม |
| 31 | 2 หน้า โดยหน้าที่ 2 เริ่มลำดับใหม่ที่ 1 |

ตรวจเพิ่มเติมว่า:

- ยอดรวมของแต่ละหน้าเป็นจำนวนรายการของหน้านั้น
- ชื่อยาวกว่า 80 ตัวอักษรไม่ล้นช่องผู้รับ
- ตาราง เส้นลายเซ็น และข้อความท้ายหน้าไม่หลุดขอบ A4
- ฟอนต์ TH Sarabun New แสดงเหมือนกันทั้งสองเบราว์เซอร์

## คำสั่งตรวจอัตโนมัติ

```powershell
node --test tests/print-layout-regression.test.mjs
```

